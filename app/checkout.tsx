import { router } from "expo-router";
import { Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { useTranslation } from "react-i18next";
import { Button, Chip, Icon, ScreenTitle } from "../src/components/ui";
import { colors, radius } from "../src/theme";
export default function Checkout() { const { t } = useTranslation(); return <ScrollView contentContainerStyle={s.page}><ScreenTitle title={t("checkout")} subtitle="Review details before placing your order" /><Text style={s.heading}>{t("address")}</Text><Pressable onPress={() => router.push("/addresses")} style={s.card}><Icon name="home-outline" /><View style={{ flex: 1 }}><Text style={s.bold}>Home · Vijay Kumar</Text><Text style={s.muted}>12-4 Lake View Colony, Hyderabad · 500081</Text></View><Icon name="chevron-forward" /></Pressable><Text style={s.heading}>Delivery slot</Text><View style={s.options}><Chip active>Tomorrow · 7–10 AM</Chip><Chip>Tomorrow · 4–7 PM</Chip></View><Text style={s.heading}>{t("payment")}</Text><View style={s.card}><Icon name="cash-outline" /><View style={{ flex: 1 }}><Text style={s.bold}>Cash on delivery</Text><Text style={s.muted}>Pay when your harvest arrives</Text></View><Icon name="checkmark-circle" /></View><View style={s.summary}><Row label={t("subtotal")} value="₹258" /><Row label={t("delivery")} value="FREE" /><Row label={t("total")} value="₹258" bold /></View><Button onPress={() => router.replace("/(tabs)/orders")}>{t("placeOrder")} · ₹258</Button><Text style={s.note}>Payment integration is intentionally a shell until the production payment adapter is configured.</Text></ScrollView>; }
function Row({ label, value, bold }: { label: string; value: string; bold?: boolean }) { return <View style={s.row}><Text style={[s.muted, bold && s.bold]}>{label}</Text><Text style={[s.bold, bold && { fontSize: 20 }]}>{value}</Text></View>; }
const s = StyleSheet.create({ page: { padding: 18, gap: 15 }, heading: { fontSize: 17, fontWeight: "900", color: colors.text, marginTop: 8 }, card: { flexDirection: "row", alignItems: "center", gap: 12, padding: 16, borderRadius: radius.lg, backgroundColor: colors.white }, bold: { fontWeight: "800", color: colors.text }, muted: { color: colors.muted, marginTop: 4, lineHeight: 20 }, options: { flexDirection: "row", flexWrap: "wrap", gap: 8 }, summary: { backgroundColor: colors.cream, padding: 18, borderRadius: radius.lg, gap: 13 }, row: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, note: { color: colors.muted, textAlign: "center", fontSize: 11, lineHeight: 17 } });
