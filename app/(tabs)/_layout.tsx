import { Tabs } from "expo-router";
import { useTranslation } from "react-i18next";
import { Icon } from "../../src/components/ui";
import { colors } from "../../src/theme";
import { useAppStore } from "../../src/store/app";
export default function TabsLayout() { const { t } = useTranslation(); const count = Object.values(useAppStore((s) => s.cart)).reduce((a, b) => a + b, 0); return <Tabs screenOptions={{ headerShown: false, tabBarActiveTintColor: colors.primary, tabBarInactiveTintColor: colors.muted, tabBarStyle: { height: 66, paddingTop: 7, paddingBottom: 7, borderTopColor: colors.border, backgroundColor: colors.white } }}><Tabs.Screen name="home" options={{ title: t("home"), tabBarIcon: ({ color }) => <Icon name="home-outline" color={color} /> }} /><Tabs.Screen name="discover" options={{ title: t("discover"), tabBarIcon: ({ color }) => <Icon name="search-outline" color={color} /> }} /><Tabs.Screen name="cart" options={{ title: t("cart"), tabBarBadge: count || undefined, tabBarIcon: ({ color }) => <Icon name="basket-outline" color={color} /> }} /><Tabs.Screen name="orders" options={{ title: t("orders"), tabBarIcon: ({ color }) => <Icon name="receipt-outline" color={color} /> }} /><Tabs.Screen name="account" options={{ title: t("account"), tabBarIcon: ({ color }) => <Icon name="person-outline" color={color} /> }} /></Tabs>; }
